import React from 'react';
import { BlocBuilder } from '@/common/bloc/BlocBuilder';
import { {{pascalCase name}}Provider } from '../bloc/{{pascalCase name}}Provider';
import {{pascalCase name}}Bloc from '../bloc/{{pascalCase name}}Bloc';
import {{pascalCase name}}State from '../bloc/{{pascalCase name}}State';

export const {{pascalCase name}}Page = () => {
  return (
    <{{pascalCase name}}Provider>
      <{{pascalCase name}}Content />
    </{{pascalCase name}}Provider>
  );
}

const {{pascalCase name}}Content = () => {
  return (
    <BlocBuilder<{{pascalCase name}}Bloc, {{pascalCase name}}State>
      builder={(state: {{pascalCase name}}State) => {
       return (<>{state.isLoading}</>);
      }
      }
    />
  );
}
